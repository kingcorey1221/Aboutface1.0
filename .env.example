VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# The MVP is local-only unless Supabase variables are configured and upload code is enabled.
# Use private buckets and signed URLs before storing reference images remotely.
